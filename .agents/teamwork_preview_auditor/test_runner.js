import os
import sys

ROOT_DIR = r"D:\Data_Lokal\Kuliah\Tri Wahyu (22518241023)\AbhinayaUNY_Web"
PUBLIC_DIR = os.path.join(ROOT_DIR, "public")
TEAM_DATA_FILE = os.path.join(ROOT_DIR, "data", "teamData.ts")

# We can run node to import and test teamData.ts using ts-node or a small node script with esbuild/tsx or compiled js!
# Let's see if we can create a small test script in node
